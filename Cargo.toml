[workspace]
resolver = "2"
members = ["engine"]

[workspace.package]
edition = "2021"
version = "0.1.0"
authors = ["Xalzeroph"]
license = "Apache-2.0"
description = "Industrial maker-first quantitative execution platform for Binance equity perpetual anchor strategies."
repository = "https://github.com/Xalzeroph/AnchorBell"

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
